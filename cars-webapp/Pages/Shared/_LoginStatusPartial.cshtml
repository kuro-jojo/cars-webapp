@if (User.Identity != null && User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Logout">
    <span class="text-white">Bonjour, @User.Identity.Name</span>
    <button type="submit" class="btn btn-danger  ml-2">Se déconnecter</button>
</form>
}
else
{
    <a class="btn btn-secondary" asp-page="/Login">Se connecter</a>
}